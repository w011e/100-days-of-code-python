class Table 
{
    tableTitle; 
    tableWidth;
    tableColumns;
    tableRows;

    /**
     * @constructor
     */
    constructor({title, widht, columns, rows } = {})
    {
        // Die Klasse sollte ein konfigurationsobject in den constructor bekommen, um dynamisch einstellbar zu sein 
        // need property, die den namen der table angibt, name soll über table stehen 
        // need property, 
    }

}